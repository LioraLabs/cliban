Board ACME has one backlog ticket for rate-limiting the login endpoint. Write
the implementation plan onto that ticket — the task breakdown only. Don't write
any code or start implementing; I just want the plan on the board so someone can
pick it up.
